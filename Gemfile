source 'https://rubygems.org'

gem 'jekyll', '~> 4.3'

group :jekyll_plugins do
  gem 'jekyll-feed'
  gem 'jekyll-sitemap'
  gem 'jekyll-seo-tag'
end

gem 'webrick', '~> 1.8'

# stdlib gems no longer bundled by default in Ruby 3.4+ that Jekyll needs.
gem 'csv'
gem 'base64'
gem 'bigdecimal'
gem 'logger'
